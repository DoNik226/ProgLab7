/*import databaseLogic.databaseElementLogic.DBCollectionLoader;
import models.LabWork;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.sql.SQLException;
import java.util.HashSet;

@SuppressWarnings("resource")
public class JDBCLoadDBTest {
    @Test
    public void test() throws SQLException, IOException {
        HashSet<LabWork> toSet = new HashSet<>();
        DBCollectionLoader<HashSet<LabWork>> dbCollectionLoader = new DBCollectionLoader<>(toSet);
        dbCollectionLoader.loadFromDB();
        System.out.println(toSet.size());
        //System.out.println(toSet.stream().findFirst().get());
    }
}*/
